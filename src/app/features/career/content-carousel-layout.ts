/** Matches Tailwind `gap-4` between content carousel cards */
export const CONTENT_CAROUSEL_GAP_PX = 16;

/** Desktop max card width + gap — arrow fallback if measurement fails */
export const CONTENT_CAROUSEL_ARROW_STEP_FALLBACK = 318 + CONTENT_CAROUSEL_GAP_PX;
